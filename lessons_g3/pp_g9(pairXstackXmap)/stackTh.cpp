#include<iostream>
#include<stack>

using namespace std;

int main(){
    string s;
    cin >> s;
    stack <char> st;
    st.push() // zakinut v top
    st.pop() // udalit top
    st.top() // cmotrit chto-tam na verhushke 
    // po suti poslenii in elemnet(last in)
    if(){
        cout << "YES";
    }else cout << "NO";
    return 0;
}

